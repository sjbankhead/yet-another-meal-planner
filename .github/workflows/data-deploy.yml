# Reusable workflow: deploy the yamp Worker AND publish the operator's plugin
# marketplace. A DATA repo (PUBLIC — it carries nothing secret) calls this from a thin
# workflow_dispatch caller — see that repo's .github/workflows/deploy.yml. The data repo
# is the operator's single control plane AND their plugin marketplace: it holds the one
# Actions secret (CLOUDFLARE_API_TOKEN, encrypted; + optional Kroger creds), the operator's
# wrangler config, and the published plugin bundle. The code repo holds NO secrets and is
# never forked for ops.
#
# Source vs config: the Worker SOURCE comes from this code repo (pinned by
# code_ref — DRY, updates propagate via @main); the operator's wrangler.jsonc
# lives in THEIR data repo and is overlaid onto the source before deploy.
#
# Zero-config (see the zero-config-deploy change):
#  - KV namespace ids and the D1 database id are auto-provisioned by `wrangler deploy`
#    (the template config ships id-less bindings) and PINNED BACK into the data repo so
#    redeploys reuse them. D1 schema migrations (migrations/d1/*.sql) are then applied
#    via `wrangler d1 migrations apply DB --remote`.
#  - The R2 corpus bucket (CORPUS) is ensured before deploy (idempotent create); the
#    authored corpus lives there, read/written by the Worker — no GitHub App or data repo
#    on the data path. The recipe index is projected by the Worker reconcile, not CI.
#  - The lower-value Kroger creds are optionally set here from data-repo secrets.
name: data-deploy

on:
  workflow_call:
    inputs:
      code_ref:
        description: Ref of caseyWebb/yet-another-meal-planner to deploy the Worker source from.
        type: string
        default: main
      config_path:
        description: Path, in the DATA repo, to the operator's wrangler.jsonc.
        type: string
        default: wrangler.jsonc
      worker_host:
        description: "Connector host (e.g. yamp.<you>.workers.dev). Used to bake https://<host>/mcp into the published plugin bundle AND to stamp the README health badge. Blank skips BOTH the plugin publish and the badge. Callers pass their WORKER_HOST repo variable here."
        type: string
        required: false
        default: ""
    secrets:
      CLOUDFLARE_API_TOKEN:
        description: "Cloudflare token with Workers + KV + D1 edit."
        required: true
      KROGER_CLIENT_ID:
        description: "Optional: Kroger client id; when set, the deploy puts it as a Worker secret."
        required: false
      KROGER_CLIENT_SECRET:
        description: "Optional: Kroger client secret; set alongside KROGER_CLIENT_ID."
        required: false

# No `permissions:` block here on purpose: a reusable workflow that REQUESTS more
# than the caller grants fails at startup. The pin-back step needs `contents: write`,
# which the CALLER grants (the template deploy.yml does); if it doesn't, the pin-back
# degrades gracefully (warns, doesn't fail the deploy). So existing callers without a
# permissions block keep working — their explicit-id configs make the pin-back a no-op.

jobs:
  deploy:
    runs-on: ubuntu-latest
    # The Worker source + build scripts + mise.toml live in _code/, but the merge /
    # build steps run from the data-repo root. Point mise at _code/mise.toml so
    # node/aube resolve from any cwd.
    env:
      MISE_GLOBAL_CONFIG_FILE: ${{ github.workspace }}/_code/mise.toml
    steps:
      # The caller's repo (the data repo) — for the operator's wrangler.jsonc, the
      # target of the KV-id pin-back commit, and the marketplace bundle the publish
      # step commits. fetch-depth: 0 (full history) so the plugin version derives from
      # `git rev-list --count HEAD` of the data repo — a shallow clone would return 1.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      # This code repo — the Worker source + build deps. Into _code/ so nothing is
      # ever committed back to the data repo (except the pinned ids, below).
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: sjbankhead/yet-another-meal-planner
          ref: ${{ inputs.code_ref }}
          path: _code

      - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
        with:
          github_token: ${{ github.token }}

      - name: Cache aube store
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.local/share/aube/store
          key: aube-store-${{ runner.os }}-${{ hashFiles('_code/aube-lock.yaml') }}
          restore-keys: aube-store-${{ runner.os }}-

      - name: Install dependencies
        run: aube ci
        working-directory: _code

      - name: Typecheck
        run: aubr typecheck
        working-directory: _code

      - name: Test
        run: aubr test
        working-directory: _code

      # Assemble the deployed config by MERGING code-level keys (triggers,
      # compatibility_*, main, observability) from the code repo with the operator's
      # owned keys (vars, KV ids, name, routes) — instead of the old wholesale `cp`
      # overlay that dropped code-level config (and silently the flyer cron). The merge
      # strips the code repo's vars/KV ids (the maintainer's) so they never reach an
      # operator. See scripts/merge-wrangler-config.mjs.
      - name: Merge wrangler config (code-level + operator-owned)
        run: node _code/packages/worker/scripts/merge-wrangler-config.mjs merge "${{ inputs.config_path }}" _code/packages/worker/wrangler.jsonc

      # The R2 corpus bucket binding (CORPUS) is bound at deploy but `wrangler deploy`
      # does not create the bucket, so ensure it exists first. Idempotent: re-creating an
      # existing bucket is a no-op success (the name is the code-level binding's fixed
      # `bucket_name`, scoped to THIS operator's Cloudflare account). The robust pin-back +
      # second-deploy idempotency guard is operator-provisioning's concern.
      - name: Ensure R2 corpus bucket exists
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        continue-on-error: true
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: _code/packages/worker
          command: r2 bucket create yamp-corpus

      # The version-skew stamp (member-app-shell): ONE code SHA, computed once, stamped
      # into BOTH sides of the contract — the SPA bundle (VITE_APP_BUILD, embedded at
      # build time below) and the deployed Worker (the --var APP_BUILD on the deploy).
      - name: Compute build id
        run: echo "BUILD=$(git -C _code rev-parse --short HEAD)" >> "$GITHUB_ENV"

      # assets/ is a build artifact (NOT committed — the esbuild bundles embed
      # environment-specific module paths, so a committed copy isn't reproducible). Build the
      # operator panel's island bundles + stylesheet (assets/admin/) so `wrangler deploy`'s
      # ASSETS binding (./assets) has them. esbuild-only, no network registry.
      - name: Build admin panel islands
        run: aubr build:admin
        working-directory: _code

      # The member SPA builds into the same assets/ root (index.html + hashed chunks +
      # the service worker), stamped with the code SHA — the assets binding serves a
      # FRESH bundle on every deploy, never a committed copy (build-automation).
      - name: Build member app
        run: aubr build:app
        working-directory: _code
        env:
          VITE_APP_BUILD: ${{ env.BUILD }}

      # The in-chat widget bundles (assets/widgets/) build into the same assets/ root so
      # `wrangler deploy`'s ASSETS binding (./assets) serves them; without this the
      # ui://recipe/card resource has no backing asset and the widget never mounts
      # (build-automation) — the same silently-dropped trap as a missing run_worker_first entry.
      - name: Build widgets
        run: aubr build:widgets
        working-directory: _code

      # Deploy. KV/D1 bindings with no id are auto-provisioned here; the recipe index is
      # projected by the Worker reconcile (no CI D1 write), not at deploy. APP_BUILD is
      # the Worker side of the member app's version-skew stamp (echoed as X-App-Build on
      # every /api response and by GET /api/version). MARKETPLACE_REPO is the calling
      # data repo — the operator's plugin marketplace by construction — stamped so the
      # member app's Connect-to-Claude modal templates the real slug with zero operator
      # setup (whoami serves it; see src/deployment.ts).
      - name: Deploy
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: _code/packages/worker
          command: deploy --var APP_BUILD:${{ env.BUILD }} --var MARKETPLACE_REPO:${{ github.repository }}

      # The Worker exists now, so secrets can be put. Only set Kroger creds if the
      # operator provided them as repo secrets — never clobber dashboard-set values
      # with empties. The App private key is intentionally NOT handled here.
      - name: Set Kroger secrets (only if provided)
        working-directory: _code/packages/worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          KID: ${{ secrets.KROGER_CLIENT_ID }}
          KSECRET: ${{ secrets.KROGER_CLIENT_SECRET }}
        run: |
          if [ -n "$KID" ] && [ -n "$KSECRET" ]; then
            printf '%s' "$KID"     | npx --yes wrangler secret put KROGER_CLIENT_ID
            printf '%s' "$KSECRET" | npx --yes wrangler secret put KROGER_CLIENT_SECRET
          else
            echo "Kroger creds not supplied as repo secrets — leaving any dashboard-set values untouched."
          fi

      # Pin auto-provisioned KV namespace ids AND the D1 database id back into the
      # operator's config so the NEXT deploy reuses the same namespaces/database. Under
      # the merge, _code/packages/worker/wrangler.jsonc is the FULL deployed config (code-level +
      # operator), so a wholesale copy-back would re-bloat the operator's slim config
      # with code-level keys — instead we surgically patch only the provisioned ids (by
      # binding) into the operator's config. The same `pin` invocation covers both binding
      # types (merge-wrangler-config.mjs pinBindingIds + bindingIdsChanged).
      #
      # Assumes `wrangler deploy` writes the provisioned ids into _code/packages/worker/wrangler.jsonc
      # in place; if a wrangler version stops doing so, fetch ids via `wrangler kv
      # namespace list` / `wrangler d1 list` instead (the pin step's input is the only
      # thing that changes).
      - name: Pin auto-provisioned KV + D1 ids back to the data repo
        env:
          CONFIG: ${{ inputs.config_path }}
        run: |
          node _code/packages/worker/scripts/merge-wrangler-config.mjs pin _code/packages/worker/wrangler.jsonc "$CONFIG"
          if git diff --quiet -- "$CONFIG"; then
            echo "No KV/D1 id changes to persist."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add "$CONFIG"
            git commit -m "chore(deploy): pin auto-provisioned KV + D1 ids"
            if ! git push; then
              echo "::warning::Could not push pinned KV/D1 ids — either the deploy caller lacks 'permissions: contents: write', or an out-of-band push moved the branch. The deploy succeeded; grant the permission (or paste the ids from wrangler.jsonc manually) so they persist."
            fi
          fi

      # Stamp the health badge into the data repo README (data-repo-health-badge): an
      # idempotent <!-- health-badge:start -->…<!-- health-badge:end --> block pointing at
      # the Worker's OPEN /health.svg card. Reuses the operator's existing WORKER_HOST
      # (passed by the thin caller, like build-plugin's mcp_url). /health.svg is open and
      # tenant-clean, so the badge is a plain public URL (no token — that's how a README
      # badge must work). OPTIONAL, like the id pin above: skipped when worker_host is
      # missing; and the ready-to-paste snippet is ALWAYS written to the job summary FIRST,
      # so a missing README or a denied push never costs an operator the manual fallback.
      - name: Stamp health badge into README (optional)
        env:
          HOST: ${{ inputs.worker_host }}
        run: |
          if [ -z "$HOST" ]; then
            {
              echo "## Health badge"
              echo ""
              echo "Not stamped — set the \`WORKER_HOST\` repo variable to enable the README status badge."
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          # Surface the snippet BEFORE any file/commit work, so neither a missing README nor a
          # denied push can rob the operator of the paste fallback.
          SNIPPET="$(node _code/packages/worker/scripts/stamp-readme-badge.mjs snippet "$HOST")"
          {
            echo "## Health badge"
            echo ""
            echo "Maintained in your README automatically when the deploy can write back; otherwise paste it in once:"
            echo ""
            echo '```markdown'
            echo "$SNIPPET"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          if [ ! -f README.md ]; then
            echo "No README.md in the data repo — skipped stamping (snippet is in the summary above)."
            exit 0
          fi
          node _code/packages/worker/scripts/stamp-readme-badge.mjs stamp README.md "$HOST" >/dev/null
          # `git diff --quiet` ignores UNTRACKED files, so stage first and check the index —
          # else a brand-new README (older-template repo) would be written but never committed.
          git add -A -- README.md
          if git diff --cached --quiet -- README.md; then
            echo "README badge already up to date."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "chore(deploy): stamp health badge in README"
            if ! git push; then
              echo "::warning::Could not push the README health badge — either the deploy caller lacks 'permissions: contents: write', or an out-of-band push moved the branch. The deploy succeeded; paste the snippet from this run's summary into your README once."
            fi
          fi

      # Apply pending D1 SCHEMA migrations (migrations/d1/*.sql) to the now-provisioned
      # database. This is the native track — `wrangler d1 migrations apply` tracks applied
      # files in D1's own `d1_migrations` table (created on first apply), distinct from the
      # `.mjs` runner's `migrations:applied` KV ledger below. Runs AFTER the pin (so the
      # database_id is in the operator config) and BEFORE the data backfill (so its tables
      # exist). A no-op when nothing is pending. The operator's CLOUDFLARE_API_TOKEN must
      # carry D1 edit — an under-scoped token surfaces as a clear failure here.
      - name: Apply D1 schema migrations
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: _code/packages/worker
          command: d1 migrations apply DB --remote

      # Build the plugin bundle with THIS operator's connector URL baked in and publish
      # it to their data repo, which serves as their plugin marketplace. This is the TAIL
      # of the deploy: it runs only after the Worker deploy above, so published skills can
      # never reference a tool the deployed Worker does not yet serve — the Worker-first
      # ordering is structural, not documented discipline. The version is the data repo's
      # own commit count: monotonic per operator (every publish is a commit), which is what
      # claude.ai's strictly-greater auto-update gate needs to re-pull. The connector host
      # comes from worker_host (passed by the thin caller, like the badge). Skipped with a
      # warning when worker_host is absent; graceful on a denied push (like the pin/badge
      # steps) so the deploy still succeeds.
      - name: Build & publish plugin to the data-repo marketplace
        env:
          HOST: ${{ inputs.worker_host }}
        run: |
          if [ -z "$HOST" ]; then
            echo "::warning::No worker_host — skipping plugin publish. Set the WORKER_HOST repo variable (and pass it as worker_host) so the deploy can bake your connector URL and publish your marketplace bundle."
            exit 0
          fi
          MCP_URL="https://$HOST/mcp"
          # 0.2.<count>: the old code-repo marketplace published the same plugin name up to
          # 0.1.126, and claude.ai gates auto-update strictly-greater per name — so the
          # data-repo era uses 0.2.x to sit above that whole 0.1.x line for every operator
          # (a data-repo commit count alone can't be relied on to clear 0.1.126). <count>
          # keeps each republish strictly newer.
          VERSION="0.2.$(git rev-list --count HEAD)"
          node _code/packages/plugin/scripts/build-plugin.mjs --out plugin/yamp --mcp-url "$MCP_URL" --version "$VERSION"
          git add plugin/yamp
          # marketplace.json (committed in the data repo, → ./plugin/yamp) ships
          # too if present; the build does not generate it.
          [ -f .claude-plugin/marketplace.json ] && git add .claude-plugin/marketplace.json
          if git diff --cached --quiet; then
            echo "Plugin bundle already up to date (v$VERSION)."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "chore(deploy): publish plugin bundle (v$VERSION)"
            # Deploys are serialized by the caller's `concurrency: group: deploy`, so this push
            # only fails on a missing write permission or an out-of-band push to the branch.
            if ! git push; then
              echo "::warning::Could not publish the plugin bundle — either the deploy caller lacks 'permissions: contents: write', or an out-of-band push moved the branch. The Worker deploy itself succeeded; grant the permission (or re-run the deploy) to publish."
            fi
            # Only when we actually published: a bundle with no manifest won't resolve as a marketplace.
            [ -f .claude-plugin/marketplace.json ] || echo "::warning::Published the plugin bundle but there is no .claude-plugin/marketplace.json — add one pointing at ./plugin/yamp (see the data-repo template) so the marketplace resolves."
          fi

      # (The one-time KV/GitHub→D1 backfill migrations have been applied and retired —
      # there is no `.mjs` data-migration step anymore. Schema lives in the D1-native
      # step above.)
      #
      # The recipe index is NO LONGER projected from CI: the Worker's scheduled reconcile
      # (src/recipe-projection.ts) reads the R2 corpus, validates it, and rebuilds the D1
      # `recipes` table every tick. After a fresh deploy the index is empty until the
      # first cron tick (~5 min) — eventual, the system's accepted model. No CI writes D1.
